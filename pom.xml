<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.farao-community.farao</groupId>
    <artifactId>farao-bom</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0-SNAPSHOT</version>

    <name>FARAO BOM</name>
    <description>Main pom file to define lib versions of FARAO related projects -- business and technical.</description>

    <modules>
        <module>powsybl-bom</module>
        <module>farao-app-bom</module>
        <module>farao-app-parent</module>
        <module>farao-lib-parent</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Business projects version -->
        <powsybl.core.version>4.3.1</powsybl.core.version>
        <powsybl.rte.core.version>3.3.1</powsybl.rte.core.version>
        <powsybl.open-loadflow.version>0.12.0</powsybl.open-loadflow.version>
        <farao.core.version>3.2.0</farao.core.version>
        <farao.rte.core.version>2.2.0</farao.rte.core.version>

        <!-- Maven plugins version -->
        <maven.compiler.version>3.8.1</maven.compiler.version>
        <maven.compiler.release>11</maven.compiler.release>
        <maven.checkstyle.version>3.1.1</maven.checkstyle.version>
        <maven.jacoco.version>0.8.5</maven.jacoco.version>
        <maven.surefire.version>2.22.2</maven.surefire.version>

        <!-- Technical dependencies version -->
        <minio.version>7.1.0</minio.version>
        <springboot.dependencies.version>2.3.6.RELEASE</springboot.dependencies.version>
        <springfox.version>2.9.2</springfox.version>
    </properties>

</project>